---
phase: 02-keepalive-probe
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/commands/probe-keepalive.js
  - src/commands/probe-report.js
  - src/services/keepalive-probe.js
  - src/services/probe-storage.js
  - src/utils/power-state-parser.js
  - tests/commands/probe-keepalive.test.js
  - tests/commands/probe-report.test.js
  - tests/services/keepalive-probe.test.js
  - tests/services/probe-storage.test.js
  - tests/utils/power-state-parser.test.js
  - src/index.js
  - docker-compose.yml
  - Dockerfile
  - package.json
  - package-lock.json
  - README.md
autonomous: true
requirements: [PROBE-01, PROBE-02, PROBE-03, PROBE-04, PROBE-05, PROBE-06]

must_haves:
  truths:
    - "All ~16 probe files are committed in a single commit on main"
    - "Commit message follows project format: feat(probe): add keepalive probe with SQLite persistence and CLI reporting"
    - "git log shows the commit includes all new source, test, config, and infra files"
    - "Remote main branch is up to date with the commit"
  artifacts:
    - path: ".git/COMMIT_EDITMSG"
      provides: "Commit message record"
      contains: "feat(probe)"
  key_links:
    - from: "git commit"
      to: "origin/main"
      via: "git push origin main"
      pattern: "origin/main"
---

<objective>
Stage all ~16 uncommitted probe files and create a single commit, then push to origin/main.

Purpose: Deliver the complete keepalive probe feature to the repository in one atomic commit per D-05.
Output: Single git commit containing all probe source, test, Docker, and config changes; remote main updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-keepalive-probe/02-CONTEXT.md
@.planning/phases/02-keepalive-probe/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage and commit all probe files in a single commit</name>
  <files>
    src/commands/probe-keepalive.js,
    src/commands/probe-report.js,
    src/services/keepalive-probe.js,
    src/services/probe-storage.js,
    src/utils/power-state-parser.js,
    tests/commands/probe-keepalive.test.js,
    tests/commands/probe-report.test.js,
    tests/services/keepalive-probe.test.js,
    tests/services/probe-storage.test.js,
    tests/utils/power-state-parser.test.js,
    src/index.js,
    docker-compose.yml,
    Dockerfile,
    package.json,
    package-lock.json,
    README.md
  </files>
  <read_first>
    - (no file read needed — git status provides current state)
  </read_first>
  <action>
Run git status to confirm the expected files are present as untracked or modified. Then stage exactly these files by name (do NOT use `git add .` or `git add -A`):

```
git add \
  src/commands/probe-keepalive.js \
  src/commands/probe-report.js \
  src/services/keepalive-probe.js \
  src/services/probe-storage.js \
  src/utils/power-state-parser.js \
  tests/commands/probe-keepalive.test.js \
  tests/commands/probe-report.test.js \
  tests/services/keepalive-probe.test.js \
  tests/services/probe-storage.test.js \
  tests/utils/power-state-parser.test.js \
  src/index.js \
  docker-compose.yml \
  Dockerfile \
  package.json \
  package-lock.json \
  README.md
```

Then commit with this exact message (per D-05 single-commit strategy and project commit format):

```
git commit -m "feat(probe): add keepalive probe with SQLite persistence and CLI reporting"
```

Do not amend any prior commit. This is a new commit.
  </action>
  <verify>
    <automated>git log --oneline -1</automated>
  </verify>
  <done>
    - git log --oneline -1 shows a commit starting with "feat(probe):"
    - git show --stat HEAD lists all 16 probe files
    - git status shows no remaining untracked or modified files from the probe set
  </done>
</task>

<task type="auto">
  <name>Task 2: Push to origin/main</name>
  <files>— (remote operation, no local file changes)</files>
  <read_first>
    - (no file read needed)
  </read_first>
  <action>
Push the commit to the remote:

```
git push origin main
```

If the push is rejected due to diverged history, do NOT force push. Instead, run `git pull --rebase origin main` and then re-push. Only use `--force` if explicitly instructed by the user.
  </action>
  <verify>
    <automated>git log --oneline origin/main -1 2>/dev/null || echo "remote check skipped (no network)"</automated>
  </verify>
  <done>
    - git push exits with code 0
    - git log origin/main -1 shows the feat(probe) commit
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → origin/main | Commit pushed to shared remote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | git push to main | accept | Single-developer repo; no branch protection required at this scale |
| T-02-05 | Information Disclosure | package-lock.json in commit | accept | No secrets in lockfile; standard practice |
</threat_model>

<verification>
After push:

```
git log --oneline -3
git show --stat HEAD
```

Expected: HEAD is feat(probe) commit, stat shows all 16 probe files, no untracked probe files remain.
</verification>

<success_criteria>
- `git log --oneline -1` output contains "feat(probe):"
- `git show --stat HEAD` lists all 16 files from the probe set
- `git push origin main` exits 0
- `git status` shows clean working tree (no probe files remaining as untracked/modified)
</success_criteria>

<output>
After completion, create `.planning/phases/02-keepalive-probe/02-02-SUMMARY.md`
</output>
